{
  "name": "ko-boxing-library",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "extract": "uv run --with pdfplumber --with pypdf python scripts/extract_pdfs.py",
    "reshape": "node scripts/build_programs.mjs",
    "audit:lessons": "node scripts/audit_lessons.mjs",
    "validate:content": "node scripts/validate_programs.mjs",
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@carbon/icons-vue": "10.130.0",
    "@carbon/styles": "1.112.0",
    "nuxt": "4.5.2",
    "sass": "^1.90.0"
  }
}
